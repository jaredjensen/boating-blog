{
  "title": "Boating Blog",
  "name": "boating-blog",
  "version": "1.0.0",
  "description": "Blog for our family boating adventures",
  "scripts": {
    "build": "gulp && cd src && bundle exec jekyll build",
    "html": "cd src && bundle exec jekyll build --watch",
    "sass": "gulp watch",
    "start": "npm-run-all --parallel html sass",
    "serve:jekyll": "npm run start && cd src && bundle exec jekyll serve --incremental --watch"
  },
  "author": "Jared Jensen",
  "dependencies": {
    "bootstrap": "4.6.0",
    "font-awesome": "4.7.0",
    "gulp": "^4.0.2",
    "gulp-cssmin": "^0.2.0",
    "gulp-header": "^2.0.9",
    "gulp-rename": "^2.0.0",
    "gulp-sass": "^4.0.2",
    "jquery": "3.6.0",
    "npm-run-all": "^4.1.5",
    "sass": "^1.52.1",
    "startbootstrap-clean-blog": "5.1.0"
  },
  "devDependencies": {
    "browser-sync": "2.26.14"
  }
}
